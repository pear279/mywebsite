# 必读需求说明

> 状态：项目唯一有效需求基线  
> 最近更新：2026-08-16  
> 适用范围：`my-website` 的设计、开发、文案、动效、验收与发布

## 0. 强制执行规则

1. 每次修改本项目之前，必须完整阅读本文件，并以本文件作为实施和验收依据。
2. 用户提出新需求时，必须在同一次修改中更新本文件，记录新增、替换或废止的规则。
3. 新需求与本文件冲突时，不得自行选择或静默覆盖，必须先向用户说明冲突并询问确认。
4. 同一主题以用户最后一次明确确认的要求为准；确认后同步更新本文件，旧规则标记为废止或直接替换。
5. 未经用户明确要求，不得为了“更像参考网站”复制对方品牌、文案、代码或独特资产；只借鉴构图、节奏、交互语言与视觉气质。
6. 调整必须建立在当前完整视觉效果上，不得因重构滚动或组件而退化成无设计的纯文字页面。
7. 完成修改后必须验证桌面端、移动端、减少动态效果、浏览器控制台、锚点、下载附件和关键 Hover/点击交互。

## 1. 产品目标

为 AI 产品经理李慧珍（pear279）搭建个人作品集网站，面向招聘者、用人经理、产品负责人和潜在合作者，帮助访客快速了解个人定位、教育背景、两段实习、三个项目、技能能力与联系方式，并下载简历。

网站需要呈现：技术理解、产品判断、结构化表达、审美能力和真实推进经验。

## 2. 视觉与品牌基线

- 整体气质：简约、高级、大气、克制、精确、有编辑感。
- Hero 参考 Karol Ortyl：灰黑雾化场域、细腻颗粒、手绘品牌字标、中心身份标签和有控制的入场动效。
- 目录、经历和项目参考 GreyMac：细线分区、编辑式排版、吸附标签、项目状态切换和强烈但克制的色块。
- 色彩系统：暖纸白、炭黑、单一荧光酸绿作为行动/激活色；项目与能力卡可使用少量实色区分。
- 标题小巧精致，重要性优先通过留白、细线、字重与版式建立，避免超大章节标题占满画面。
- 技能卡片保持当前“两排彩色能力卡”视觉样式，不改成图片卡片。

## 3. 导航与信息架构

顶部导航必须包含且顺序如下：

1. 首页
2. 关于我
3. 工作经历
4. 项目作品
5. 技能特长
6. 联系我
7. 下载简历

要求：

- 当前章节必须有激活状态。
- 移动端可以折叠为菜单，但不得删除任何导航项。
- 下载简历直接下载 `public/李慧珍-AI产品经理-简历.pdf`。
- 关于我右侧目录承担时间线功能，按时间顺序列出项目和实习，链接到对应内部步骤。

## 4. Hero

- Hero 中心主视觉固定使用透明 PNG `/pear-279-handdrawn.png`，原始尺寸 `1672 × 941`；必须保留原图的白色手写线稿、颗粒磨损和透明通道，不得以字体或重新绘制的 SVG 代替。
- 标题图中 `P–e` 与 `7–9` 必须保持清晰分离，不得出现横穿字符内部的多余连接线或斜向交叉线。
- PNG 仅替换 Hero 中心的 `pear 279`；滚动后左上角标识与页脚继续使用现有紧凑 SVG 字标，不随本次替换改变。
- 左上角固定标识需要足够清晰，并通过深色底托同时适应深色、浅色章节。
- 删除所有 Hero 边缘元信息，包括顶部左右和底部左右的小字。
- Hero 只保留：中心 `pear 279`、身份标签、下滑提示、灰黑雾化/颗粒背景。
- 桌面端 PNG 宽度约为视口的 `72vw`，最大 `1060px`；移动端宽度不超过 `460px`。使用约 `1672:500` 的可见窗口居中承载原图，以消化图片上下透明留白且不裁断线稿。
- 身份标签使用“酸绿眉标＋纸白职位＋半透明深色价值主张”的原创组合，固定文案为 `AI PRODUCT · HUMAN FIRST`、`AI 产品经理`、`把复杂 AI 变成自然体验`。
- 背景以深灰到黑灰的径向渐变建立摄影棚打光感，并保留远近两层雾场、底部深度层和细颗粒；不得引入参考站的品牌黄红贴纸、文案、绘图画布或独特资产。
- 保留 React Bits `ClickSpark`：白色、`sparkSize=12`、`sparkRadius=20`、`sparkCount=9`、`duration=500`。
- Hero PNG 使用轻微缩放淡入；继续保留细指针设备上的低幅双层视差，以及“标签先退、字标后退、雾层最后消散”的滚动退场；动效不得影响可读性。
- Hero 沿用 GSAP、Lenis 与 ClickSpark，不为装饰效果新增 Framer Motion、Lottie、Canvas 绘图或其他重叠动画依赖。

## 5. 一次滚轮章节导航

桌面端采用“一次滚轮推进一个叙事步骤”，章节顺序为：

1. 首页
2. 关于我
3. 工作经历
4. 项目作品
5. 技能特长
6. 联系我

内部步骤：

- 工作经历：腾讯 → CodeXpert。进入工作经历时展示腾讯；再次向下滚动切换到 CodeXpert；下一次进入项目作品。
- 项目作品：moodseed → 东方游 → SoundLens。完成三个项目后再进入技能特长。
- 向上滚动按相反顺序返回，不能跳过内部内容。

行为要求：

- 每次有效滚轮输入只执行一次切换，切换期间锁定重复输入，避免连续跳页。
- 外部章节通过 Lenis 平滑滚动到章节顶部；内部步骤保持章节固定，仅对内容做淡出、位移、缩放和替换。
- 支持 `ArrowUp`、`ArrowDown`、`PageUp`、`PageDown`、空格键和 `Home`/`End`。
- 导航与关于我目录锚点可以直接进入目标章节或内部项目。
- 宽屏桌面端（当前断点为 `1041px` 且设备具有精确指针）使用 GSAP Observer 统一滚轮输入；移动端和平板保留自然触摸滚动和章节边界吸附，避免长内容或浏览器手势被锁死。
- `prefers-reduced-motion` 下保留导航逻辑，但内部切换和章节移动改为即时或极短过渡。

## 6. 关于我

- 展示 AI 产品经理定位、建筑学背景、优化后的个人简介和教育经历。
- 右侧目录纵向排列，默认弱化，Hover/Focus 时高亮并轻微放大。
- 目录列出三个项目和两段实习，按时间顺序排列并可直达对应步骤。
- 移动端目录改为自然纵向列表，不得隐藏。

## 7. 工作经历

- 固定展示两段实习：腾讯浏览器 Agent 组、CodeXpert 云端编码智能体。
- 每个步骤包含：公司/产品名称、岗位、时间、工作描述和能力标签。
- 使用不同颜色纯色视觉块作为图片占位；Hover 时显示小字关键词并轻微放大。
- 左下角显示当前“名称＋时间”吸附标签，并包含内部进度；切换步骤时同步更新和轻微放大。
- 吸附标签不得遮挡正文、标签或其他链接。

## 8. 项目作品

- 固定展示三个项目：moodseed、东方游、SoundLens。
- 每个步骤包含：标题、时间、描述、标签、视觉展示框和 GitHub 链接。
- 简历缺少的内容可使用明确占位符或项目仓库列表链接，不得伪造信息。
- 视觉块 Hover 时显示关键词，主文字轻微缩放/旋转，保持当前实色编辑风格。
- 左下角吸附标签同步显示当前项目名称、时间和内部进度，不得遮挡正文与链接。

## 9. 技能特长

- 保持当前两排彩色文字能力卡视觉样式。
- 两排错位并向相反方向循环自动播放。
- Hover/Focus 时当前轨道暂停，当前卡片轻微放大；离开后继续。
- 内容需要同时表达产品能力、AI 能力、数据能力、协作能力和快速交付能力。
- 不能退化为普通文字列表或通用图标卡片网格。

## 10. 联系方式与页脚

- 小巧简约地展示邮箱 `3500788359@qq.com`、电话 `159 5210 5455` 和 GitHub `@pear279`。
- 邮箱、电话和 GitHub 必须可点击。
- 页脚保留品牌字标、身份、年份和返回顶部入口。

## 11. 技术与质量要求

- 技术栈：Next.js、React、TypeScript、纯 CSS、Lenis、GSAP、ScrollTrigger、Observer、React Bits ClickSpark。
- 可用设计依赖：Motion、Material Web、Font Awesome React；AI Elements CLI 作为按需添加 AI 组件的开发依赖。新依赖已经安装不代表必须使用，禁止为了展示技术栈叠加功能重合的库。
- GSAP 必须使用 React 安全的生命周期和清理方式；Observer、监听器、ticker 和 Lenis 必须在卸载时销毁。
- 动画优先使用 `transform` 和 `opacity`，避免滚动期间动画布局属性。
- 桌面和移动端不得出现横向溢出。
- 浏览器控制台不得出现运行时错误或动画警告。
- 支持键盘、Focus 状态和 `prefers-reduced-motion`。
- `design.md` 用于记录设计理由和经验；本文件用于记录不可违背的产品需求。二者冲突时以本文件为准。
- 开发服务器运行时不得同时执行 `next build` 共用同一个 `.next` 目录。

### 11.1 设计资源研究规则

- 全局 Skill `/Users/pear279/.codex/skills/design-resource-library` 是本项目设计研究入口。
- 之后涉及视觉设计、动效、组件选型、交互调整、页面重构或设计审核时，必须主动调用该 Skill，并按任务选择 2—4 个匹配资源进行研究。
- 参考资源用于提取构图、层级、节奏、动效逻辑和交互反馈，不得直接复制其他网站的品牌、文案、代码或独特资产。
- 设计资源、API、安装方式和授权可能变化，实际使用前必须浏览官方页面复核。
- 采用的关键参考和取舍需要写入 `design.md`；新增长期设计规则需要同步更新本文件。

## 12. 当前验收清单

- [x] 导航包含全部七项，并在技能章节正确激活。
- [x] Hero 中心已使用用户提供的透明手写线稿 PNG。
- [x] Hero 已删除全部边缘元信息。
- [x] Hero PNG 在桌面与移动端均按可见线稿区域自适应缩放，透明留白不挤压身份标签。
- [x] Hero 身份标签、远近雾层、低幅视差和分阶段滚动退场有效。
- [x] ClickSpark 点击有效。
- [x] 一次滚轮只推进一个外部章节或内部步骤。
- [x] 经历 2 步、项目 3 步可前进、后退、锚点直达。
- [x] 吸附标签不遮挡内容。
- [x] 技能卡保持当前样式，轮播、暂停、放大有效。
- [x] PDF 下载、邮箱、电话和 GitHub 链接有效。
- [x] 桌面端和移动端无横向溢出、无控制台错误。
- [x] 减少动态效果模式可用。
- [x] `design.md` 已与最终方案同步。

## 13. 变更记录

### 2026-08-16

- 建立本文件作为项目唯一需求基线。
- 确认 Hero 使用小写 `pear 279` 并保留一个空格。
- 确认删除 Hero 全部边缘元信息。
- 确认桌面端采用一次滚轮推进一个叙事步骤。
- 确认工作经历使用 2 个内部步骤、项目作品使用 3 个内部步骤。
- 确认技能卡保持当前彩色文字卡片样式。
- 完成桌面端一次滚轮章节导航、内部步骤切换、反向返回、键盘控制和锚点直达。
- 工作经历与项目作品重构为固定章节画布内的内容替换；移动端保留全部内容的自然连续浏览。
- 完成桌面 1440×900 与移动 390×844 验收，确认无横向溢出和浏览器控制台错误。
- 建立全局 `design-resource-library` Skill，收录动效、设计系统、组件、图标、资产和灵感网站，并规定设计任务主动调用。
- 安装 Motion、Material Web、Font Awesome React 与 AI Elements CLI；Lenis、GSAP 和 React Bits 沿用现有集成。
- 基于 Karol Ortyl 首屏逻辑重构 Hero：放大签名字标并强化留白，新增远近雾层、底部深度层与原创三段身份标签；移动端使用同路径上下堆叠构图。
- 将 Hero 动效收敛为路径书写、细指针低幅视差和分阶段滚动退场；拒绝复制参考站黄红贴纸、品牌文案、Canvas 绘图和 Framer/Lottie 技术栈。
- 完成 1440×900、390×844 与 360×640 Hero 验收；确认桌面单步滚轮往返、移动菜单、锚点、ClickSpark、PDF、reduced-motion 和控制台状态正常。
- 根据浏览器批注将 `pear 279` 从细线签名改为原创粗描边涂鸦气泡字：字符内部镂空，并加入随机白点和 `1—7` 微型编号；桌面与移动端继续复用同一套路径几何。
- 根据第二轮浏览器批注完全废止规整几何气泡方案：重画为七条连续、开放、自交叉的马克笔路径，字符分别倾斜并使用 `4—6px` 不同线宽；编号扩展到 `1—12` 并非机械散布，背景改为黑灰径向摄影棚光场。
- 根据用户提供的透明背景 PNG，将 Hero 中心手写 `pear 279` 替换为 `/pear-279-handdrawn.png`；按可见线稿高度重新计算响应式承载窗口，保留左上角与页脚 SVG 标识及其余页面结构。
- 清理 Hero 标题图中 `P–e` 和 `7–9` 的杂乱交叉线；保留原始 `1672 × 941` 尺寸、白色磨损质感和真实 Alpha 透明通道。
